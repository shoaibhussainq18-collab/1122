<div class="max-w-4xl mx-auto px-4 py-12">
  <h1 class="text-3xl font-black text-gray-900 mb-6 font-montserrat">{{ page.title }}</h1>
  <div class="prose text-gray-700 leading-relaxed">{{ page.content }}</div>
</div>
